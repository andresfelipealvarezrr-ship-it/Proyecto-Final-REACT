Estas carpetas van a contener:

pages → las 4 vistas (Inicio, Productos, Agregar, Editar)
components → componentes reutilizables (Navbar, ProductCard, etc.)
context → el estado global con Context API

